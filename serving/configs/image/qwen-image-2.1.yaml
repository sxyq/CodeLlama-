model:
  name: Qwen-Image-2.1
  path: /data/vllm/ImageModel/Qwen-Image-2.1

runtime:
  backend: diffusers
  dtype: bfloat16
  local_files_only: true
  lazy_load: true
  cpu_offload: true

service:
  host: 0.0.0.0
  port: 8011
  idle_timeout_seconds: 600

gpu:
  exclusive_large_model: true
  check_ollama_before_load: true

# STAGING: 配置准备阶段，不启动模型、不启动服务
